name: build workflow

on: 
  push:
    branches:
      - main

env: 
  PROJECT_VERSION: 1.0.0

jobs:
  main_job:
    runs-on: ubuntu-latest
    steps:
    - name: update version
      run: $PROJECT_VERSION++
